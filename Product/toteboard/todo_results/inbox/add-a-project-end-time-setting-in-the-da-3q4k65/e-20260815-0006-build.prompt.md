Perform a normal implementation build for the TODO item below. Title, brief, expanded description, and plan are authoritative context (all four; see Context). Make the required code changes directly in the repository. On completion, write a concise build summary to the response file: what changed, files touched with line numbers, how it was verified, and any follow-ups. List major touch points with file path and line numbers so later review guides have concrete anchors. Also write the request echo and trace to their files; then write the result sidecar and release your claim.

After the implementation is complete and verified, the last processing item is to commit the current worktree. Write a commit message consistent with this request (title, brief, plan) and with CHANGELOG.md if the change warrants a changelog note. Stage only files related to this work. Do not commit unrelated dirty files. If there is nothing to commit, say so in the build summary and do not create an empty commit.

Context
Title: Add a project End Time setting in the database, relevant API services, and UI.
Brief: This is an optional setting that can be None. If None, no time limits are displayed to users or those comparing options.

The end time for the project is stored as a standard datetime in the database, allowing None if no end time was explicitly specified. 

In the UI it should be presented on the project settings as a date, or none, with click to change option where appropriate. The user would get a calendar chooser to specify a future date when the project voting closes.

The user interface on shares and in the voting interface first page and where applicable throughout, should mention 'Comparisons on this project open N more [days|weeks|months] on m/d/y', switching to approximate weeks above 20, and months above 60 days.

Always set the expiration for 11:59 PM on the date the user specifies.
The date should be checked in the voting section and no votes are accepted more than 1 hour after expiration. Starting one hour before expiration, add a prominent (but not overpowering) notice that comparison collection ends soon.

Adjust messaging and vocabulary based on our terminology preferences.
Node ID: add-a-project-end-time-setting-in-the-da-3q4k65
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)
Expanded: Add an optional project end time as a nullable datetime so a project can run with no deadline; when unset, hide all time-limit copy from settings, shares, and the comparison UI. When set, store 11:59 PM on the chosen date, show that date in project settings with a click-to-change calendar for a future close day, and surface remaining-time messaging on shares and the comparison first page (and elsewhere it applies): “Comparisons on this project open N more [days|weeks|months] on m/d/y,” using days, approximate weeks after 20 days, and months after 60. Reject new comparisons more than one hour after expiration, and starting one hour before close show a prominent but restrained notice that comparison collection ends soon—using comparison/collection wording, not “votes,” in user-facing text.

- Scope: nullable DB datetime, API read/write, settings date (or none) + calendar, share/comparison remaining-time copy, server-side cutoff with 1-hour grace, last-hour notice.
- None means no displays and no enforcement; existing projects should default to no end time.
- Always persist 11:59 PM on the selected date; confirm timezone (project, user, or UTC) before implementing.
- Remaining-time units: days, then ~weeks above 20 days, ~months above 60; include the calendar date.
- Open: who can set/clear the date; behavior after close beyond blocking submissions (results still viewable?); exact last-hour notice copy.
Plan: I'll explore the codebase to map project settings, comparison APIs, and UI so the plan matches existing patterns.Checking the knowledge graph and exploring project settings, comparison APIs, and UI in parallel.I'll query the graph and read the key settings, vote, and share files to pin down implementation steps.## Plan: optional project end time

### 1. Implementation steps

1. **DB** — Add nullable `end_time` (`DateTime`, no server default) on `customerproject` via Alembic after head `k2l3m4n5o6p7`. Existing rows stay `NULL` (no deadline, no copy, no enforcement).

2. **Model** — On `CustomerProject` (`src/db/models/customer_projects.py`), add `end_time: datetime | None` next to `disabled`, same naive `DateTime` pattern as `deleted_date` / customer expiration fields.

3. **Normalize + helpers** — Shared backend helper (e.g. next to project vote utils):
   - Persist **23:59:00** on the chosen calendar date (see timezone option below).
   - `collection_is_closed(end_time, now)` → `True` iff `end_time` is set and `now > end_time + 1 hour`.
   - `collection_ending_soon(end_time, now)` → `True` iff set and `end_time - 1 hour <= now <= end_time + 1 hour`.
   - Reject copy: **“Comparison collection has ended.”** (not “votes”). Reuse for both session and share paths. Leave `disabled` as a separate lock.

4. **API write** — Add `end_time: datetime | None` to `CustomerProjectForm`. Set it on create (`customer_project_create`). On update, **special-case this field**: the current `if value is not None` loop cannot clear it. Always apply `end_time` when the key is present (including `null`). Restrict set/clear to the same rule as other project settings (`customer_admin` + `update_mode`). Reject past dates on write (today allowed → that day’s 11:59 PM).

5. **API read** — GET already returns the full row. Also expose `end_time` (and optionally `collection_ending_soon`) on:
   - `_project_settings_payload` (`app_project_vote_events.py`)
   - `SharedProjectSummary`, `_project_settings`, `_load_project_summary` (`app_shared_link_ext_access.py`)
   so probe + share UIs do not need a second fetch.

6. **Enforce cutoff** — Same gates as `project.disabled`:
   - Session: `get_or_create_session_participant`, `next_group`, `complete_group`
   - Share: `shared_project_vote_next_group`, `shared_project_vote_complete_group`
   After grace, refuse new groups/completions. Do **not** block results, report shares, or already-stored comparisons. `disabled` and `end_time` both close input independently.

7. **Client types/API** — Add `end_time?: string | null` to `CustomerProject` and `SharedProjectSummary`. Pass it through `useProjectsApi` create/update bodies.

8. **Settings UI** (`client/pages/projects/[id]/edit.vue`) — On the Project tab, beside **Close input**:
   - Show **None** or the close date (`m/d/y`).
   - Click value / Change opens a future-date calendar (`v-menu` + Vuetify `VDatePicker`; no new dep). Include Clear → `null`.
   - Save with existing Update/Save. Display date only; server forces 11:59 PM.
   - There is no click-to-edit pattern today; this control is the first.

9. **Remaining-time copy** — Shared helper (e.g. `useFormat` / small composable). If `end_time` is null, render nothing. Else:
   - `Comparisons on this project open N more [days|weeks|months] on m/d/y`
   - Days while remaining ≤ 20; ~weeks after 20 (`round(days/7)`); ~months after 60 (`round(days/30)`).
   Surfaces: `probe.vue` under the subtitle; `ShareAccessGate` / `ShareVoteSession` first page; invite line in `shares.vue` `buildInvite` when set. Optional overview chip on `projects/[id]/index.vue`.

10. **Last-hour notice** — Compact tonal `v-alert` (same density as private-participation / locked alerts) on `probe.vue` and `ShareVoteSession.vue`: **“Comparison collection ends soon.”** Show from one hour before `end_time` through the 1-hour grace window. After grace, reuse locked pattern: **“Input closed. New comparisons cannot be recorded.”** Hide remaining-time sentence once closed.

11. **Tests** — `test_manage_projects.py`: create/update/clear `end_time`, 11:59 normalization, past-date reject, non-admin 403. Vote + share-ext tests: accept until `end_time + 1h`, reject after; `disabled` still independent; null `end_time` never blocks. Keep internal path/table names (`vote`); user-facing `failure_reason` uses comparison wording.

12. **Verify** — Backend pytest for the files above; frontend typecheck/lint if the repo has them.

---

### 2. Clarifying questions

1. **Timezone** — There is no project or user TZ. Should 11:59 PM be **UTC on the picked calendar date**, or **the admin’s local 11:59 PM** stored as UTC?
2. **Who can set/clear** — Same as other settings (`customer_admin` only), or also members / only the creator?
3. **After close** — Confirm results, report shares, and overview stay available; only new comparisons are blocked (plus hide/disable **Start comparing** after grace)?
4. **Last-hour copy** — Use **“Comparison collection ends soon.”** or include the date/time?
5. **Past dates** — Allow admins to set a past date to close immediately (after grace), or only future dates?
6. **Weeks/months rounding** — Confirm `round(days/7)` and `round(days/30)`, and singular/plural (`1 more week` vs `1 more weeks`).

---

### 3. Options and trade-offs

| Topic | Options | Recommendation |
|--------|---------|----------------|
| Clock | (A) 23:59 UTC on the selected date. Simple, date-stable, can close earlier/later than the admin’s evening. (B) 23:59 in the browser TZ → store UTC. Matches “the date the user specifies.” (C) Add a project TZ field. Correct long-term, out of scope. | **(B)** unless you want a later TZ setting. Always **show the calendar date**, never a clock, in UI copy. |
| Clear `end_time` | (A) Special-case `None` in the update loop. (B) Sentinel (`""` / omit vs explicit null). (C) Separate clear endpoint. | **(A)** — smallest change; document that `end_time` is the one nullable field that must apply `None`. |
| Date picker | (A) Native `input type="date"`. (B) Vuetify `VDatePicker` in a menu. | **(B)** — matches “click to change” + calendar; Vuetify is already in the app. |
| Who can edit | (A) `customer_admin` (current update rule). (B) Any tenant member. | **(A)** — same as Close input and other settings. |
| After close | (A) Input-only lock (results still open). (B) Also treat like a full project shutdown. | **(A)** — matches `disabled` today; report/vote_view shares stay readable. |
| Last-hour copy | (A) Short: “Comparison collection ends soon.” (B) Longer with date. | **(A)** — remaining-time line already has the date; keep the alert restrained. |
| Remaining units | Client-only vs server-computed string. | **Client helper** from `end_time`; server only stores the datetime. |

**Defaults if unanswered:** (B) local 11:59 → naive UTC; admin-only set/clear; results remain viewable; last-hour copy (A); future dates only (today OK); rounded weeks/months with correct pluralization.

Output paths
request: todo_results/inbox/add-a-project-end-time-setting-in-the-da-3q4k65/2026-08-15T18-34-34-586Z-build.request.md
response: todo_results/inbox/add-a-project-end-time-setting-in-the-da-3q4k65/2026-08-15T18-34-34-586Z-build.response.md
trace: todo_results/inbox/add-a-project-end-time-setting-in-the-da-3q4k65/2026-08-15T18-34-34-586Z-build.trace.log
result: todo_results/inbox/add-a-project-end-time-setting-in-the-da-3q4k65/2026-08-15T18-34-34-586Z-build.result.json