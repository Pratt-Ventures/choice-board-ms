Perform a normal implementation build for the TODO item below. Title, brief, expanded description, and plan are authoritative context (all four; see Context). Make the required code changes directly in the repository. On completion, write a concise build summary to the response file: what changed, files touched with line numbers, how it was verified, and any follow-ups. List major touch points with file path and line numbers so later review guides have concrete anchors. Also write the request echo and trace to their files; then write the result sidecar and release your claim.

After the implementation is complete and verified, the last processing item is to commit the current worktree. Write a commit message consistent with this request (title, brief, plan) and with CHANGELOG.md if the change warrants a changelog note. Stage only files related to this work. Do not commit unrelated dirty files. If there is nothing to commit, say so in the build summary and do not create an empty commit.

Context
Kind: Work Package (multi-item; sub-items may not be technically cohesive)
Title: Minor UI Tweaks - Project Editor
Overview: (none)
Brief: (none)
Node ID: minor-ui-tweaks-project-editor-1p6iw0
Category: FAST TRACK - AUTO RUN
Notes: (none)
Work items (actionable, in order):
1. Rank all choice
   Brief: On pages in the settings hide the 'Rank Top Half' function. On all pages in t he settings and project status, change the 'Rank All' nomenclature to 'Rank Entire Options List'
2. Confidence bar title on comparison pages
   Brief: Change confidence bar title/legend from 'Current Ranking Confidence' to 'Your Current Ranking Confidence'
3. New group starting interstitial page
   Brief: Show the category title (and description if present) above the question that will be presented. This is reversing the order, so its title - description - Question you will see, rather than question followed by title (and may description, which should be shown)
4. 'Start Comparing' should be 'Start Comparing' or 'Continue Comparing'
   Brief: The 'Start Comparing' action on the Project Overview should adapt to whether the present user has already supplied at least one group (or partial). It should be labled 'Continue Comparing' if any prior comparisons were done (by this user) in the project.
Expanded: Apply four small, mostly cosmetic UI changes across the project editor and comparison flow: hide the "Find Top Half" ranking-mode option and relabel "Rank All" as "Rank Entire Options List" wherever decision modes appear (settings, status/overview), retitle the confidence bar legend to "Your Current Ranking Confidence", reorder the group interstitial so the category title and description appear above the upcoming question, and make the overview's "Start Comparing" button read "Continue Comparing" when the current user has any prior (even partial) comparisons in the project.

- Scope is label/visibility and display-order changes only — no changes to ranking logic, mode values (`rank_all`, `find_top_half` remain valid internally), confidence computation, or comparison scheduling.
- "Rank Top Half"/"Find Top Half" hide must cover every surface that offers decision modes (edit settings toggle in `client/pages/projects/[id]/edit.vue`, plus any status/summary displays); existing projects saved with `find_top_half` need a defined fallback behavior.
- Interstitial change affects where the factor/category context block renders relative to the question text; description should render only when present.
- "Continue Comparing" requires reliable per-user prior-participation detection on Project Overview (identity via logged-in user vs. share/session participant); partial groups count as prior activity.
- Verify no tests or e2e flows assert on old labels ("Rank All", "Start Comparing", confidence-bar title).
- Open: should hidden top-half mode also be excluded from hints/captions, does "project status" include shared/read-only views, and what label applies when a user has prior activity but the project input is closed?
Plan: ## Plan: Minor UI Tweaks – Project Editor

### 1) Implementation steps

1. **Ranking-mode toggle (edit settings)** — `client/pages/projects/[id]/edit.vue`
   - Remove the `Find Top Half` button (line 95) from the Decision-mode `v-btn-toggle`; relabel `Rank All` → `Rank Entire Options List` (line 96).
   - Drop/replace the `find_top_half` hint branch in `rankingModeHint` (lines 864–869).
   - Legacy fallback: keep `find_top_half` as a valid stored value (load mapping at lines 1255–1258 unchanged). If a loaded project is already `find_top_half`, render that toggle entry disabled/badged "legacy mode" instead of silently hiding it, so the current selection stays visible and users can switch away; save sends whatever is selected — no auto-migration.
2. **Mode nomenclature on status surfaces** — update `'Rank all'` → `'Rank Entire Options List'` (keep `'Pick one'`): overview chip + summary hero `client/pages/projects/[id]/index.vue:22,66`; edit review-tab chip `edit.vue:474`; template-save copy `edit.vue:578`. Optionally add a shared `modeLabel()` helper in `client/utils/ranking.ts` to replace the repeated ternaries.
3. **Confidence bar legend** — `client/components/RankingConfidenceBar.vue`: caption line 12 → "Your Current Ranking Confidence"; update matching aria-label (line 9). Both consumers (`probe.vue:58`, `ShareVoteSession.vue:143`) pick this up automatically.
4. **Group interstitial reorder** — `client/components/CompareQuestionHeader.vue` (intro variant only): move the criterion banner above the kicker/question block so order becomes title → description → question; render description inline when present (already available via `factorDetailsCopy()` from `criterion.description`, currently dialog-only); keep description omitted when empty. Leave `variant="pair"` rendering untouched. Consumers needing no change: `probe.vue:129-142`, `ShareVoteSession.vue:202-215`.
5. **Adaptive Start/Continue Comparing** — `client/pages/projects/[id]/index.vue`: add computed `myObservationCount > 0 ? 'Continue comparing' : 'Start comparing'` bound to the head-action button (line 43). `my_observation_count` (server: `src/api/app_customer_project_management.py:576,624`) is per logged-in user and includes partial-flushed votes, satisfying "even partial counts"; no API change needed. The "Recommended next action" card already routes repeat users to results, so no change there.
6. **Verification** — grep repo for stale strings (`Rank All`, `Start Comparing` as fixed labels, `Current ranking confidence`); run client unit tests (`node --test client/utils/*.test.ts` — none assert changed labels today) and the Playwright suite in `e2e/` (no specs assert these labels; only the generic 'Comparing' eyebrow in `e2e/helpers/gate.ts:66`); manually exercise edit → review → overview → probe intro and one share/vote session.

### 2) Clarifying questions

1. Scope of "project status": include the projects-list rows (`client/pages/projects/index.vue:102,157,200`) and templates page ("Rank all options", `templates/index.vue:90`), or strictly settings + project overview?
2. For legacy `find_top_half` projects, is hidden-but-preserved (with a small "legacy mode" indicator when editing) acceptable, versus silently remapping them to another mode?
3. When input is closed, the compare button is hidden entirely — confirm closed-input users never see "Continue Comparing" (per the open question in the brief).

### 3) Options & trade-offs

- **Hide vs. remove top-half mode**: hard-delete the option/button everywhere is cleaner but breaks display for saved projects; **recommended**: hide for new selection + preserve value with legacy indicator (matches expanded note that `find_top_half` remains internally valid).
- **Deduplicate mode labels via shared helper vs. inline ternaries**: helper adds a tiny abstraction but prevents future drift across ~7 call sites; recommended given the rename touches many files.
- **Per-user detection source**: reuse summary's `my_observation_count` (zero backend work, already partial-inclusive) vs. new endpoint/share-session-aware check; recommended reuse — share participants use the share flow, not the customer overview, so logged-in identity suffices.
- **Interstitial reorder location**: change inside `CompareQuestionHeader` (one component, both probe and share flows consistent) vs. reordering in each page; recommended component-level change scoped to `variant === 'intro'`.

Output paths
request: todo_results/inbox/minor-ui-tweaks-project-editor-1p6iw0/2026-08-21T14-27-33-864Z-build.request.md
response: todo_results/inbox/minor-ui-tweaks-project-editor-1p6iw0/2026-08-21T14-27-33-864Z-build.response.md
trace: todo_results/inbox/minor-ui-tweaks-project-editor-1p6iw0/2026-08-21T14-27-33-864Z-build.trace.log
result: todo_results/inbox/minor-ui-tweaks-project-editor-1p6iw0/2026-08-21T14-27-33-864Z-build.result.json