Perform a normal implementation build for the TODO item below. Title, brief, expanded description, and plan are authoritative context (all four; see Context). Make the required code changes directly in the repository. On completion, write a concise build summary to the response file: what changed, files touched with line numbers, how it was verified, and any follow-ups. List major touch points with file path and line numbers so later review guides have concrete anchors. Also write the request echo and trace to their files; then write the result sidecar and release your claim.

After the implementation is complete and verified, the last processing item is to commit the current worktree. Write a commit message consistent with this request (title, brief, plan) and with CHANGELOG.md if the change warrants a changelog note. Stage only files related to this work. Do not commit unrelated dirty files. If there is nothing to commit, say so in the build summary and do not create an empty commit.

Context
Title: Change share security mode selection interaction
Brief: On share invite, the '*show all options' should be changed to 'show all*'
The ''show common options' should be 'show basic' and should float near the pull down, like the existing show all does.
Node ID: change-share-security-mode-selection-int-18hh8h
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)
Expanded: I'll locate the share-invite security-mode UI so the expansion matches the current labels and layout.On the create-share invite dialog, retitle the access-mode toggle so collapsed state reads `show all*` (was `*show all options`) and expanded state reads `show basic` (was `show common options`). Keep both controls as the existing text-button next to the Access mode select; `show basic` must sit beside the pulldown like `show all*` does today, not get pushed to the far end of the row. Scope is the invite UI in `client/pages/projects/[id]/shares.vue` (labels plus the `justify-space-between` layout when all modes are shown).

- Scope: label copy on both toggles; keep `show basic` adjacent to the select (drop or stop applying `justify-space-between` on `.access-mode-row` when expanded).
- Do not change access-mode lists, defaults, or backend share security behavior.
- Preserve the leading/trailing asterisk treatment as specified (`show all*` / `show basic`).
- Check e2e/unit selectors that match the old strings (`*show all options`, `show common options`).
- Confirm whether edit-share or other dialogs reuse the same pattern; if not, leave them alone.
- Open: should `show basic` also use an asterisk, or stay unadorned as written?
Plan: (none)

Output paths
request: todo_results/inbox/change-share-security-mode-selection-int-18hh8h/2026-08-19T00-43-33-861Z-build.request.md
response: todo_results/inbox/change-share-security-mode-selection-int-18hh8h/2026-08-19T00-43-33-861Z-build.response.md
trace: todo_results/inbox/change-share-security-mode-selection-int-18hh8h/2026-08-19T00-43-33-861Z-build.trace.log
result: todo_results/inbox/change-share-security-mode-selection-int-18hh8h/2026-08-19T00-43-33-861Z-build.result.json