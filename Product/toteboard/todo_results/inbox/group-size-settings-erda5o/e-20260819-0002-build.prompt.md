Perform a normal implementation build for the TODO item below. Title, brief, expanded description, and plan are authoritative context (all four; see Context). Make the required code changes directly in the repository. On completion, write a concise build summary to the response file: what changed, files touched with line numbers, how it was verified, and any follow-ups. List major touch points with file path and line numbers so later review guides have concrete anchors. Also write the request echo and trace to their files; then write the result sidecar and release your claim.

After the implementation is complete and verified, the last processing item is to commit the current worktree. Write a commit message consistent with this request (title, brief, plan) and with CHANGELOG.md if the change warrants a changelog note. Stage only files related to this work. Do not commit unrelated dirty files. If there is nothing to commit, say so in the build summary and do not create an empty commit.

Context
Title: Group size settings
Brief: The group size settings should remember whether they had defaults changed. When new items are added, it should adjust to the FJ defaults unless they were explicitly set.
Node ID: group-size-settings-erda5o
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)
Expanded: I'll look up how group size settings and FJ defaults work so the expansion matches the codebase.Checking how group size and FJ defaults are stored so the expansion is accurate.Group size (`option_questions_per_group` / `factor_questions_per_group`) must track whether the user overrode the Ford–Johnson default, not just store a number. Today those fields are always persisted as ints and reclamped, so adding items cannot tell “still on default” from “user chose this budget.” If the value was never explicitly set, new items should retarget the current FJ default for the new `n`; if it was set, keep the user’s value (still clamp to `question_budget_bounds`).

- Scope: persist an explicit/default flag (or nullable until first edit) for option and factor group size; on item add/remove, apply FJ default only when not explicit.
- Recompute using `default_questions_per_group` / `ford_johnson_budget(n)` and existing clamp helpers; do not rewrite explicit values except to stay in bounds.
- Cover create/update APIs, vote session partitioning, and the project settings UI so the control can show default vs custom.
- Consider: treat matching the current FJ number as still-default, or only as default until the user touches the control?
- Open: should item deletion also retarget defaults? Independent flags per option vs factor? Migration for existing projects that already have stored ints?
Plan: (none)

Output paths
request: todo_results/inbox/group-size-settings-erda5o/2026-08-19T16-38-16-280Z-build.request.md
response: todo_results/inbox/group-size-settings-erda5o/2026-08-19T16-38-16-280Z-build.response.md
trace: todo_results/inbox/group-size-settings-erda5o/2026-08-19T16-38-16-280Z-build.trace.log
result: todo_results/inbox/group-size-settings-erda5o/2026-08-19T16-38-16-280Z-build.result.json