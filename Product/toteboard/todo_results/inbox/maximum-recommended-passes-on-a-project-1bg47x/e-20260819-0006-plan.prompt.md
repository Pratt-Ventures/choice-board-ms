You are processing a TODO planning request. No changes to the environment, repository files, or configuration are expected or allowed; your only permitted writes are the four specified output files under the inbox directory. Using the title, brief, and expanded description below (Expanded is authoritative prior output; see Context), produce a concise execution plan containing: (1) numbered implementation steps, (2) clarifying questions, (3) options and trade-offs with a recommendation. Write only the plan to the response file, the request echo to the request file, and a brief trace to the trace file; then write the result sidecar and release your claim.

Context
Title: Maximum recommended passes on a project
Brief: This appears to default to either 3 or 1 over the minimum and allows the lower value to be saved, even below the minimum.

Always make the minimum value in the up/down the on page min expected passes. When saving, ensure it is similarly enforced.

Change the default to the min number of passes. Project creators can increase if desired.
Node ID: maximum-recommended-passes-on-a-project-1bg47x
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)
Expanded: The maximum-recommended-passes field on a project currently defaults to either a hardcoded 3 or one above the page’s minimum expected passes, and the spinner/save path can persist a value below that on-page minimum. The control’s lower bound should always be the page’s min expected passes, and save should reject or clamp anything below that. Default the field to the minimum expected pass count so creators start at the floor and can raise it if they want.

- Scope: spinner min, default value, and save validation for max recommended passes on create/edit.
- Tie the up/down floor to the same on-page min expected passes used elsewhere.
- Enforce the same floor on save (clamp or reject); do not allow under-min persistence.
- Default new/unsaved values to min expected passes, not min+1 or 3.
- Confirm whether existing below-min records should be migrated, clamped on load, or left until next save.
- Keep UI and API/schema constraints consistent across create and edit.

Output paths
request: todo_results/inbox/maximum-recommended-passes-on-a-project-1bg47x/2026-08-19T16-59-56-960Z-plan.request.md
response: todo_results/inbox/maximum-recommended-passes-on-a-project-1bg47x/2026-08-19T16-59-56-960Z-plan.response.md
trace: todo_results/inbox/maximum-recommended-passes-on-a-project-1bg47x/2026-08-19T16-59-56-960Z-plan.trace.log
result: todo_results/inbox/maximum-recommended-passes-on-a-project-1bg47x/2026-08-19T16-59-56-960Z-plan.result.json