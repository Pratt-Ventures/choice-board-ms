You are processing a TODO expansion request from a worklist file. No changes to the environment, repository files, or configuration are expected or allowed; your only permitted writes are the four specified output files under the inbox directory. Expand from the title and brief description (see Context and Title: Context field masking
Brief: Add an option in config.settigns that specifies a string list of fields that are masked in either customer or user data structures returned in a request for context session api call. 

These calls are normally provided at login and give the client UI helpful information to understand user and customer state. Not all data in the customer or user data is appropriate in this general situation.

Implement the configuration setting and update the context calls.

The default value is "stripe_settings," though it would allow multiple values comma separated. Make sure this is clear the comment with the config entry. 

The conversion to a list should be done in the config load process, not on each use. If there are no masked fields, specified by an empty string or None, an empty list stored in the config.settings after load.) into exactly one short paragraph (2–4 sentences) followed by 3–7 concise bullets covering scope, key considerations, and open questions. Be concise. Write only the expansion to the response file, the request echo to the request file, and a brief trace to the trace file; then write the result sidecar and release your claim.

Context
Title: Context field masking
Brief: Add an option in config.settigns that specifies a string list of fields that are masked in either customer or user data structures returned in a request for context session api call. 

These calls are normally provided at login and give the client UI helpful information to understand user and customer state. Not all data in the customer or user data is appropriate in this general situation.

Implement the configuration setting and update the context calls.

The default value is "stripe_settings," though it would allow multiple values comma separated. Make sure this is clear the comment with the config entry. 

The conversion to a list should be done in the config load process, not on each use. If there are no masked fields, specified by an empty string or None, an empty list stored in the config.settings after load.
Node ID: context-field-masking-3cbp6u
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)

Output paths
request: todo_results/inbox/context-field-masking-3cbp6u/2026-08-15T18-16-07-127Z-expand.request.md
response: todo_results/inbox/context-field-masking-3cbp6u/2026-08-15T18-16-07-127Z-expand.response.md
trace: todo_results/inbox/context-field-masking-3cbp6u/2026-08-15T18-16-07-127Z-expand.trace.log
result: todo_results/inbox/context-field-masking-3cbp6u/2026-08-15T18-16-07-127Z-expand.result.json