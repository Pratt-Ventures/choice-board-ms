You are processing a TODO expansion request from a worklist file. No changes to the environment, repository files, or configuration are expected or allowed; your only permitted writes are the four specified output files under the inbox directory. Expand from the title and brief description (see Context and Title: Configure new item in PVF framework for TESTING database
Brief: When testing, spurious records often end up in the configured database for develoment. In production, tests would never be run on the same database.

However, we'd like to keep the dev databases cleaner. In the PVF framework, add an additional database connection for pytest runs. This  should use similarly named and positioned configuration information. Set the inital values to the same database server, but adding _test to the database name. The alternative database name parameter is selected when PYTEST_ACTIVE is true, which should be set whenever a test is being run.

The alembic configuration should always be evaluated against the primary live database configuration for generation purposes. The updates or rollbacks must be applied to both databases. Verify that PYTEST_ACTIVE is used during testing, update agents.md to indicate we use a distinct database, often within the same server (a config practice you can assume in the dev environment).

If more information is needed, include the questions in the Expanded output and they will be answered there.) into exactly one short paragraph (2–4 sentences) followed by 3–7 concise bullets covering scope, key considerations, and open questions. Be concise. Write only the expansion to the response file, the request echo to the request file, and a brief trace to the trace file; then write the result sidecar and release your claim.

Context
Title: Configure new item in PVF framework for TESTING database
Brief: When testing, spurious records often end up in the configured database for develoment. In production, tests would never be run on the same database.

However, we'd like to keep the dev databases cleaner. In the PVF framework, add an additional database connection for pytest runs. This  should use similarly named and positioned configuration information. Set the inital values to the same database server, but adding _test to the database name. The alternative database name parameter is selected when PYTEST_ACTIVE is true, which should be set whenever a test is being run.

The alembic configuration should always be evaluated against the primary live database configuration for generation purposes. The updates or rollbacks must be applied to both databases. Verify that PYTEST_ACTIVE is used during testing, update agents.md to indicate we use a distinct database, often within the same server (a config practice you can assume in the dev environment).

If more information is needed, include the questions in the Expanded output and they will be answered there.
Node ID: configure-new-item-in-pvf-framework-for-2u48sv
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)

Output paths
request: todo_results/inbox/configure-new-item-in-pvf-framework-for-2u48sv/2026-08-21T13-06-11-965Z-expand.request.md
response: todo_results/inbox/configure-new-item-in-pvf-framework-for-2u48sv/2026-08-21T13-06-11-965Z-expand.response.md
trace: todo_results/inbox/configure-new-item-in-pvf-framework-for-2u48sv/2026-08-21T13-06-11-965Z-expand.trace.log
result: todo_results/inbox/configure-new-item-in-pvf-framework-for-2u48sv/2026-08-21T13-06-11-965Z-expand.result.json