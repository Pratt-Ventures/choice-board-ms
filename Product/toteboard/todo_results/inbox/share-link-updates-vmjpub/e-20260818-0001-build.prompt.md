Perform a normal implementation build for the TODO item below. Title, brief, expanded description, and plan are authoritative context (all four; see Context). Make the required code changes directly in the repository. On completion, write a concise build summary to the response file: what changed, files touched with line numbers, how it was verified, and any follow-ups. List major touch points with file path and line numbers so later review guides have concrete anchors. Also write the request echo and trace to their files; then write the result sidecar and release your claim.

After the implementation is complete and verified, the last processing item is to commit the current worktree. Write a commit message consistent with this request (title, brief, plan) and with CHANGELOG.md if the change warrants a changelog note. Stage only files related to this work. Do not commit unrelated dirty files. If there is nothing to commit, say so in the build summary and do not create an empty commit.

Context
Kind: Work Package (multi-item; sub-items may not be technically cohesive)
Title: Share link updates
Overview: (none)
Brief: Refining the share link process
Node ID: share-link-updates-vmjpub
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)
Work items (actionable, in order):
1. Ease of use in share link page
   Brief: At the bottom of the share list, add another 'New Share Link' button so the user doesn't have to look around for it.
2. email vs copy
   Brief: If an email is sent, do not autocopy the share text
3. Login page
   Brief: When a code is required for login, but it is unknown whether an email has already been sent (a link from an emailed code would count as code known), show a clear action button saying 'Email Access Code' with a small link text underneath that says, 'already have a code'. 

If a code is present on invocate, or user has requested an email from that page, show the field to enter the code, as is shown today, and linked text underneath that says 'send/resend code to email'
Expanded: This work package refines share-link and access-code login UX so actions are easier to find and less ambiguous. It adds a second New Share Link control at the bottom of the share list, skips auto-copy when a share is emailed, and splits the login page into a clear “email a code” first step versus the existing code-entry step. The three items are independent UI changes and can ship separately.

- Scope: share-list page (duplicate New Share Link), post-send copy behavior (email vs clipboard), login when a code is required but not known vs already present or requested.
- Treat an emailed-code link as “code known”: show the code field, not Email Access Code.
- After Email Access Code (or if a code is already on the URL), keep today’s code field and add “send/resend code to email”; hide the primary Email Access Code button.
- Do not auto-copy share text when an email is sent; keep copy for non-email share actions.
- Confirm placement/styling of the extra New Share Link button and the small “already have a code” / resend links against existing share and login components.
- Open: should “already have a code” deep-link into the code field without sending mail, and should resend reuse the same email/rate-limit path as today?
Plan: (none)

Output paths
request: todo_results/inbox/share-link-updates-vmjpub/2026-08-18T11-47-27-005Z-build.request.md
response: todo_results/inbox/share-link-updates-vmjpub/2026-08-18T11-47-27-005Z-build.response.md
trace: todo_results/inbox/share-link-updates-vmjpub/2026-08-18T11-47-27-005Z-build.trace.log
result: todo_results/inbox/share-link-updates-vmjpub/2026-08-18T11-47-27-005Z-build.result.json