New system configuration will specify an AI service, access key, and model (Opencode GO/Zen first), enabling server-orchestrated agent features without exposing keys or model details to clients; BYOK client endpoints come later. Once a problem title and description exist, AI can prefill suggested factors for the user to edit or delete. Separately, a single AI participant can answer the project’s maximum groups via a sequence of concise, grouping- and information-gain-biased prompts (same order logic as humans); those extra groups and a special status become an AI baseline in final results.

- Scope: system-level AI config; factor prefill; one AI participant for max groups; server-only orchestration
- Keep credentials and model identity off the client; treat BYOK as a later phase
- Reuse existing human prompt sequencing (grouping + systematic information-gain bias)
- Persist AI responses with distinct group/status so results can isolate the baseline
- Open: enablement UX, rate/cost limits, failure/retry, and how AI groups are labeled vs. human groups