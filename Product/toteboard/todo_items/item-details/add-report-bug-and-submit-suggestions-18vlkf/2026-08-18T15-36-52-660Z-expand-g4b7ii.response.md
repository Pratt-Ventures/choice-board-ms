Add Report Bug and Submit Suggestion from the session pulldown (logged-in users only), with matching User Communication APIs, two SQLModel tables, rate limits, and admin/user status UIs.

- Scope: session-menu dialogs (bug + suggestion + Check Submission Status), User Communication submit/retrieve/modify/soft-delete endpoints mirrored per type, MAX_REPORTS_PER_DAY (default 10, +1 buffer then discard+log), user status page and system-admin paginated lists/edit overlay.
- Models follow existing SQLModel patterns: id, customer_id, user_id, form fields, ack/response/resolution date+user+note, created/modified/soft-delete dates; attachments optional.
- Auth: any user submits/views own (customer admin: customer-wide); system admin paginated retrieve, modify dates/notes, delete any; user soft-delete own only before admin action.
- Prefer shared table/service/UI baselines; add functional and security tests; keep pages consistent with the style guide.
- Open: attachment storage vs paste-only; whether customer admin can see notes; exact log_entry shape for discarded extras.