Perform a normal implementation build for the TODO item below. Title, brief, expanded description, and plan are authoritative context (all four; see Context). Make the required code changes directly in the repository. On completion, List major touch points with file path and line numbers so later review guides have concrete anchors.

After the implementation is complete and verified, the last processing item is to commit the current worktree. Write a commit message consistent with this request (title, brief, plan) and with CHANGELOG.md if the change warrants a changelog note. Stage only files related to this work. Do not commit unrelated dirty files. If there is nothing to commit, say so in the build summary and do not create an empty commit.

You may edit repository source files as required for this build. Do not edit todo/ board storage except if explicitly told.
This is extension-owned direct execution. Do not claim queue work, call TODO MCP tools, create claim files, write queue result sidecars, or release a claim. The host records your complete reply and owns lifecycle state.
This is a fresh session with no prior conversation turns. Complete the task in this single request.
Do not use MCP todo tools and do not write claim/inbox files under todo/todo_results — the host captures your full reply.

Context
Title: Style of Join this decision box is still rounded with clipping.
Brief: Make sure styles match other dialogs, and especially don't clip text.
Node ID: style-of-join-this-decision-box-is-still-tb81f9
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)
Expanded: I'll look up the Join this decision box and how other dialogs are styled so the expansion is accurate.No exact match yet. Searching for the join-decision dialog and overflow/clipping styles.Found the join box in `ShareVoteSession.vue`. Comparing it with the other dialogs next.The join box is `ShareAccessGate`. Checking its styles against other dialogs.The “Join this decision” gate is `ShareAccessGate` on the vote share landing (`ShareVoteSession`), still a `glass-card` with `rounded="xl"` (`--pc-radius` 16px) instead of the moderate `pc-dialog-card` treatment used by other dialogs (12px). That extra rounding plus card `overflow: hidden` clips titles, field labels, alerts, and focus rings. Align the gate with other dialogs and keep text fully visible without changing access-gate behavior.

- Scope: `ShareAccessGate.vue` markup/classes and `.share-gate` / surface rules in `client/assets/styles/main.scss`; same component also gates “Open full results”.
- Match `pc-dialog-card`: ~12px radius, drop `rounded="xl"`, do not copy dialog `overflow: hidden` onto this in-page card.
- Prevent clipping: `overflow: visible` (as on `.share-surface-card`) or scroll only the body; check Vuetify field labels, tonal alerts, and focus rings.
- Out of scope: gate logic, copy, and post-unlock comparison UI.
- Verify vote and report landings, long subtitle/message, and every field combination (name/email/password/code).
- Open: restyle report gate the same way, and whether a 12px card is enough or it should become a real `v-dialog`.
Plan: (none)

## Output
After implementing, commit the worktree if there is anything to commit, then return only the build summary in your reply. Include the commit hash and message, or say nothing to commit.