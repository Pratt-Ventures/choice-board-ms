You are processing a TODO expansion request No changes to the environment, repository files, or configuration are expected or allowed; Expand from the title and brief description (see Context and Title: AI Agent voting
Brief: When the AI agents are voting the skip and tie options are supposed to be removed from the prompt if a threshold is hit.

Verify that logic. They combined value should stop around 15%, rounded up, of the group size.

Prompts should be examined and refined, given the AI is reponding with SKIP 100% of the time in early tests. The default prompt probably needs, 'Based on information about the project, make the best recommended choice for the user." -- or something similar to get it to answer.) into exactly one short paragraph (2–4 sentences) followed by 3–7 concise bullets covering scope, key considerations, and open questions. Be concise.

Do not modify repository files, configuration, or toteboard/ board storage.
This is extension-owned direct execution. Do not claim queue work, call TODO MCP tools, create claim files, write queue result sidecars, or release a claim. The host records your complete reply and owns lifecycle state.
This is a fresh session with no prior conversation turns. Complete the task in this single request.
Do not use MCP todo tools and do not write claim/inbox files under toteboard/todo_results — the host captures your full reply.

Context
Title: AI Agent voting
Brief: When the AI agents are voting the skip and tie options are supposed to be removed from the prompt if a threshold is hit.

Verify that logic. They combined value should stop around 15%, rounded up, of the group size.

Prompts should be examined and refined, given the AI is reponding with SKIP 100% of the time in early tests. The default prompt probably needs, 'Based on information about the project, make the best recommended choice for the user." -- or something similar to get it to answer.
Node ID: ai-agent-voting-15jxk3
Category: FAST TRACK - AUTO RUN
Notes: (none)

## Output
Return only the expansion text in your reply.