You are processing a TODO expansion request No changes to the environment, repository files, or configuration are expected or allowed; Expand from the title and brief description (see Context and Title: Email Token for 2fa and share verification security
Brief: Add a paramter token_security_digits_only to the pvf_settings.py configuration. It should default to False.  

If this is true, the tokens included in emails to verify users login or share access would be restricted to digits only, with the digits_only parameter passed to get_random_baseN_value. This value has the same semantics as the parameter, so it can be passed directly. 

For less secure environments and applications, it provides a user convenience tradeoff.) into exactly one short paragraph (2–4 sentences) followed by 3–7 concise bullets covering scope, key considerations, and open questions. Be concise.

Do not modify repository files, configuration, or todo/ board storage.
This is extension-owned direct execution. Do not claim queue work, call TODO MCP tools, create claim files, write queue result sidecars, or release a claim. The host records your complete reply and owns lifecycle state.
This is a fresh session with no prior conversation turns. Complete the task in this single request.
Do not use MCP todo tools and do not write claim/inbox files under todo/todo_results — the host captures your full reply.

Context
Title: Email Token for 2fa and share verification security
Brief: Add a paramter token_security_digits_only to the pvf_settings.py configuration. It should default to False.  

If this is true, the tokens included in emails to verify users login or share access would be restricted to digits only, with the digits_only parameter passed to get_random_baseN_value. This value has the same semantics as the parameter, so it can be passed directly. 

For less secure environments and applications, it provides a user convenience tradeoff.
Node ID: email-token-for-2fa-and-share-verificati-3dwy3o
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)

## Output
Return only the expansion text in your reply.