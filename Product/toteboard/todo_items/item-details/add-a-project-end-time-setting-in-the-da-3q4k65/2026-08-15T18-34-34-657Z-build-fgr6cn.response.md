I'll start by mapping project settings, comparison APIs, and UI patterns so the end-time work matches existing design.Graphify is for questions, not this build. I'll map the project, vote, and share surfaces next.Reading the project form, vote/share gates, and settings UI next so the field and cutoff match existing patterns.Implementing backend helpers, model, migration, and API next, then the UI and tests.Running the migration, then the new and related tests.UI and OpenAPI updated. Staging only this work and committing.**fb038d8** — Add optional project comparison close date

Nullable `end_time` on projects; None means no deadline, copy, or cutoff. Local 11:59 PM is stored as naive UTC. Admins set/clear it. After `end_time + 1h`, new comparisons are rejected; results stay readable.

**Touch points**
- Model: `src/db/models/customer_projects.py:74`
- Migration: `src/alembic/versions/l3m4n5o6p7q8_add_project_end_time.py`
- Helpers: `src/utils/project_end_time.py:5`, `:33`, `:47`
- API write: `src/api/app_customer_project_management.py:83`, `:199`, `:542`
- Session cutoff: `src/api/app_project_vote_events.py:283`, `:312`, `:347`
- Share cutoff/summary: `src/api/app_shared_link_ext_access.py:82`, `:256`, `:438`, `:490`
- Settings calendar: `client/pages/projects/[id]/edit.vue:94`
- Remaining-time / last-hour: `client/utils/projectEndTime.ts`, `probe.vue:10`, `ShareVoteSession.vue:28`, `ShareAccessGate.vue:7`
- Tests: `src/tests/test_project_end_time.py`, `test_manage_projects.py`, `test_project_votes.py`, `test_share_ext_access.py`