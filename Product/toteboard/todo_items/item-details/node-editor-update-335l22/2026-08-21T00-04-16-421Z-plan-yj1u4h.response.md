I'll inspect the node editor and where "View Plan" is rendered so the plan matches the current layout."View Plan" only showed up in toteboard artifacts — I'll search the app UI and graph for the node-editor chrome.