I'll inspect the node editor around "View Plan" so the plan matches existing layout and interaction.Graph exists; I'll query it and inspect the node-editor UI for View Plan, prompt/response indicators, and node types.