You are processing a TODO expansion request No changes to the environment, repository files, or configuration are expected or allowed; Expand from the title and brief description (see Context and Title: UI and Share Access Flows
Brief: Ensure the UI the functions with all available share security modes. Some are turned off to simply the interface, but should function in the app.

Also, on the page where a key is required on an invite - there is confusion when a user clicks the link and goes to a page with the 'enter code' hint, even though it says 'if received already'.  The link should go to the page with the behavior having a clear 'Send Code to Email' button and a small 'already have code' option. When linking from an email with a code, the enter code box should appear populated.) into exactly one short paragraph (2–4 sentences) followed by 3–7 concise bullets covering scope, key considerations, and open questions. Be concise.

Do not modify repository files, configuration, or todo/ board storage.
This is extension-owned direct execution. Do not claim queue work, call TODO MCP tools, create claim files, write queue result sidecars, or release a claim. The host records your complete reply and owns lifecycle state.
This is a fresh session with no prior conversation turns. Complete the task in this single request.
Do not use MCP todo tools and do not write claim/inbox files under todo/todo_results — the host captures your full reply.

Context
Title: UI and Share Access Flows
Brief: Ensure the UI the functions with all available share security modes. Some are turned off to simply the interface, but should function in the app.

Also, on the page where a key is required on an invite - there is confusion when a user clicks the link and goes to a page with the 'enter code' hint, even though it says 'if received already'.  The link should go to the page with the behavior having a clear 'Send Code to Email' button and a small 'already have code' option. When linking from an email with a code, the enter code box should appear populated.
Node ID: ui-and-share-access-flows-fa29t9
Category: SLOW TRACK - GATED RELEASE - Defaults to Expand, Plan, Build
Notes: (none)

## Output
Return only the expansion text in your reply.