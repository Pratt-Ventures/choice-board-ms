Perform a normal implementation build for the TODO item below. Title, brief, expanded description, and plan are authoritative context (all four; see Context). Make the required code changes directly in the repository. On completion, List major touch points with file path and line numbers so later review guides have concrete anchors.

After the implementation is complete and verified, the last processing item is to commit the current worktree. Write a commit message consistent with this request (title, brief, plan) and with CHANGELOG.md if the change warrants a changelog note. Stage only files related to this work. Do not commit unrelated dirty files. If there is nothing to commit, say so in the build summary and do not create an empty commit.

You may edit repository source files as required for this build. Do not edit todo/ board storage except if explicitly told.
This is extension-owned direct execution. Do not claim queue work, call TODO MCP tools, create claim files, write queue result sidecars, or release a claim. The host records your complete reply and owns lifecycle state.
This is a fresh session with no prior conversation turns. Complete the task in this single request.
Do not use MCP todo tools and do not write claim/inbox files under todo/todo_results — the host captures your full reply.

Context
Title: Submission k of n message on bug/report submission
Brief: Only show the message after 1/3 the limit.
Change the text of the brief message after submission to say, 'We limit submissions in a 24 hour period due to our capacity. You have submitted k of n as of now.'
Node ID: submission-k-of-n-message-on-bug-report-3ab3n5
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)
Expanded: I'll look up the current submission-limit copy so the expansion matches the real flow.Checking the current quota copy and when it appears.After a successful bug or idea submission, the snackbar currently always shows “Submission k of n (per day) has been submitted.” Change that brief so it appears only once the user has used more than one-third of the 24-hour cap, and replace the wording with “We limit submissions in a 24 hour period due to our capacity. You have submitted k of n as of now.” Keep the existing hard-limit discard and over-limit warning behavior unless those paths share the same copy helper.

- Scope: `quotaCopy` in `client/components/SubmissionForm.vue` (used by both bug and suggestion submits); gate display on `submitted_in_window` vs `max_per_day`.
- Show the k-of-n line only when `k > n/3` (or `k >= ceil(n/3)`—pick one and apply consistently).
- Below the threshold, keep a generic success message; do not surface quota on every early submit.
- Preserve the discard snackbar (`stored: false`) and the separate daily-limit warning; only change the brief quota line.
- Update any tests that assert the old always-on copy or expect quota on the first submit.
- Open: floor vs ceil for “1/3 the limit,” and whether a rejected over-limit submit should still show the new k-of-n sentence.
Plan: (none)

## Output
After implementing, commit the worktree if there is anything to commit, then return only the build summary in your reply. Include the commit hash and message, or say nothing to commit.