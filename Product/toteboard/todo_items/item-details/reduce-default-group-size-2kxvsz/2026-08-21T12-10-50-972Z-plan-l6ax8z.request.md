You are processing a TODO planning request. No changes to the environment, repository files, or configuration are expected or allowed; Using the title, brief, and expanded description below (Expanded is authoritative prior output; see Context), produce a concise execution plan containing: (1) numbered implementation steps, (2) clarifying questions, (3) options and trade-offs with a recommendation.

Do not modify repository files, configuration, or toteboard/ board storage.
This is extension-owned direct execution. Do not claim queue work, call TODO MCP tools, create claim files, write queue result sidecars, or release a claim. The host records your complete reply and owns lifecycle state.
This is a fresh session with no prior conversation turns. Complete the task in this single request.
Do not use MCP todo tools and do not write claim/inbox files under toteboard/todo_results — the host captures your full reply.

Context
Title: Reduce default group size
Brief: When configuring a project, the default group size is pinned at the ford-johnson predicted full sort maximum. The groups often seem too large and confidence is reached before that point, especially in top-n situations.

Reduce the default group size to two-thirds (67%) of the ford johnson estimate, subject any contraints or minimums. The range will not be changed.

Keep the ranges the same,
Node ID: reduce-default-group-size-2kxvsz
Category: FAST TRACK - AUTO RUN - Defaults to Expand, Plan, Buld
Notes: (none)
Expanded: When configuring a project, the default comparison group size is currently pinned to the Ford–Johnson predicted full-sort maximum, which often overshoots: confidence is typically reached earlier, especially for top-n. Change the default to two-thirds (67%) of that Ford–Johnson estimate, still applying existing constraints and minimums so the value cannot go invalid or below allowed floors. Do not change the configurable range (min/max); only the initial default should move. Existing projects and already-saved group sizes should be left as-is unless they currently inherit this default at configure time.

- Scope: default group-size initialization only (Ford–Johnson estimate × 0.67), then clamp with current min/constraints; leave min/max range UI and validation unchanged.
- Apply the 67% reduction before clamping so floors still win when the scaled value is too small.
- Confirm rounding (e.g. floor vs round) and that top-n vs full-sort share the same default rule.
- Do not retune Ford–Johnson itself, confidence logic, or persisted project settings.
- Open: should 67% apply only at first configure, or also when resetting defaults? Any tests/docs that assert the old full-sort maximum default?

## Output
Return only the plan text in your reply.